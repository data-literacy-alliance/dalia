version: '3.1'

services:
  instance:
    ports:
      - '127.0.0.1:${EXPOSE_PORT}:3000'
#      - '3000:3000'
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_ENV: ${BUILD_ENV}
      
    # container_name: dev-frontend

# TODO should be in the same network with backend?
